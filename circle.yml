dependencies:
  pre:
    - make; cd ../; git clone https://github.com/cpputest/cpputest.git; cd cpputest; ./autogen.sh; ./configure; make; sudo make install; cd ../testCircleCi; ls
  override:
    - ls
test:
  override:
    - pwd
checkout:
  post:
    