dependencies:
  pre:
    - cd ../; git clone https://github.com/cpputest/cpputest.git; cd cpputest; ./autogen.sh; ./configure; make; sudo make install;
  override:
    - cd ../testCircleCi;
test:
  override:
    - make && make test
checkout:
  post:
    # - git submodule init
    # - git submodule update