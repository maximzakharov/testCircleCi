dependencies:
  pre:
    - cd ../; git clone https://github.com/cpputest/cpputest.git; cd cpputest; ./autogen.sh; ./configure; make; sudo make install; cd ../testCircleCi;
  override:
    - make && make test
test:
  override:
    - pwd
checkout:
  post:
    # - git submodule init
    # - git submodule update