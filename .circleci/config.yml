version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:latest
    environment:
      IMAGETAG: latest
    working_directory: ~/.
    steps:
      - run: echo "This is pipeline ID << pipeline.id >>"
